All_users = ["victor", "tosin", "percy", "felix"]
def  register_user(name,):

All_users.append(name)

 def  delete_user(name):
       All_users.remove(name)
       
 def  total_user():
     return len(All_users)
